# 交互图：Footprint Map

**职责：** 描述用户在 Markdown 日记中加载、查看和操作单日足迹地图时的页面状态、转换与异常降级。

## 交互图

```mermaid
stateDiagram-v2
    [*] --> 打开Markdown日记
    打开Markdown日记 --> 识别足迹代码块: 已安装footprint-map适配器
    打开Markdown日记 --> 显示普通代码块: 未安装适配器

    识别足迹代码块 --> 加载数据: 解析source路径
    加载数据 --> 校验与排序: GeoJSON可读
    加载数据 --> 错误降级: 文件缺失或无法解析
    校验与排序 --> 渲染互动地图: 存在有效点位
    校验与排序 --> 空状态: 没有有效点位

    渲染互动地图 --> 浏览地图: 自动适配全部点位
    渲染互动地图 --> 等待稳定布局: 阅读模式容器尚未完成布局
    等待稳定布局 --> 浏览地图: 地图加载完成且容器尺寸稳定
    浏览地图 --> 浏览地图: 拖动或缩放
    浏览地图 --> 恢复整日视野: 点击适配视野
    恢复整日视野 --> 浏览地图
    浏览地图 --> 选择到访点: 点击照片标记
    选择到访点 --> 横向查看照片: 地图下方更新照片组
    横向查看照片 --> 横向查看照片: 左右滑动
    横向查看照片 --> 查看大图: 单击照片或键盘确认
    查看大图 --> 查看大图: 滚轮、触控板或双指捏合缩放
    查看大图 --> 查看大图: 放大后拖动
    查看大图 --> 横向查看照片: 关闭按钮、背景或Esc
    横向查看照片 --> 选择到访点: 点击另一照片标记

    浏览地图 --> 重新加载: GeoJSON或笔记已更新
    重新加载 --> 加载数据

    错误降级 --> 查看静态图: fallback存在
    错误降级 --> 查看错误详情: fallback不存在
    空状态 --> 查看错误详情: 查看无效项
    显示普通代码块 --> 查看静态图: Markdown含标准降级图
    查看静态图 --> 打开独立HTML: 点击互动地图链接

    浏览地图 --> [*]: 关闭或切换笔记
    查看静态图 --> [*]: 关闭或切换笔记
    查看错误详情 --> [*]: 关闭或切换笔记
```

## 生成与插入

```mermaid
flowchart LR
    A[在笔记中放置光标] --> B[执行从照片生成足迹]
    B --> C[记录光标或选区结束位置]
    C --> D[异步读取照片并生成GeoJSON]
    D --> E{已有footprint-map代码块?}
    E -->|否| F[在记录位置插入代码块]
    E -->|是| G[保留现有代码块]
```

## 产品反馈

```mermaid
flowchart LR
    A{反馈入口} -->|设置页| B[查看完整反馈说明与隐私提醒]
    A -->|Obsidian 地图面板标题区右上方| C[点击低强调反馈操作]
    B --> D[点击分享反馈]
    C --> D
    D --> E[在独立浏览器页面打开 Tally]
    E --> F{用户是否提交?}
    F -->|是| G[主动填写需求或问题及可选联系方式]
    F -->|否| H[关闭页面，不发送反馈内容]
```

- 反馈入口不要求或读取 Footprint Map 数据；插件不会自动附带照片、坐标、GeoJSON、设置或诊断信息。
- 地图面板入口位于标题区右上方、“适配全部点位”旁边，使用当前界面语言和辅助视觉层级；它与地图操作按钮保持间距并使用更弱的文字样式。
- standalone 不注入反馈回调，因此不显示 Tally 入口。
- 用户未点击按钮时不访问 Tally；用户只打开或关闭表单时不改变 Vault。
- Tally 无法访问时，插件设置与所有本地足迹功能仍可继续使用。

## 主任务路径

1. 用户打开包含 `footprint-map` 代码块的日记。
2. 插件读取代码块中的 `source`，加载 GeoJSON 并从早到晚排序点位。
3. 地图首次显示时自动适配所有有效点位；高德模式按 `0.25` 级细分选择保证照片卡完整可见的最大比例，同时渲染第一张照片缩略图、圆心位于卡片右上角的序号徽标、底部时间、直线虚线和箭头。
4. 用户拖动或缩放地图，也可一键恢复整日视野。
5. 用户点击照片点位，地图下方的独立浏览区切换为该地点全部照片，并可横向滑动；单击照片打开大图浮框。
6. 笔记或 GeoJSON 更新后，当前地图应在不重启 Obsidian 的情况下重新渲染。

## 地图内交互

- **拖动：** 改变当前视野，不改变足迹数据。
- **缩放：** 通过地图 `+`/`−` 控件、移动端双指手势、Mac 触控板捏合，或按住 `Command`/`Ctrl` 后滚动调整比例；无修饰键双指滚动或鼠标滚轮继续滚动 Markdown 页面，不改变地图级别。
- **适配视野：** 将所有有效点位恢复到当前地图容器内；高德模式以 `0.25` 级递进拉近，直到下一细分级别无法完整容纳全部照片卡和徽标。
- **到访点：** 以首图作为 1.5 cm 方形缩略图，序号徽标圆心位于卡片右上角并主要处于图片外侧，底栏只显示第一张照片时间；点击切换选中态。
- **照片：** 不在地图画布上打开点位弹层；地图下方独立区域横向排列所选地点的全部照片，单图高度不超过 4 cm，可左右滑动。单击照片打开居中大图浮框；滚轮、触控板和双指捏合围绕操作位置在 `1×–6×` 之间缩放，放大后可用鼠标或单指拖动且不得拖出空白边界。按 `Esc`、点击背景或关闭按钮退出，键盘聚焦照片后按 Enter/空格也可打开；`+`/`-` 可缩放，`0` 恢复。
- **顺序轨道：** 地图下方显示同样的 1→2→3 时间摘要，即使地图被拖动也可理解当日顺序。

## 异常与降级

- `source` 缺失：在原地显示字段级错误，不请求默认网络地址。
- GeoJSON 整体无效：显示错误摘要与降级图。
- 部分点位无效：显示其他点位，并列出被忽略数量及原因。
- 照片丢失：显示点位、时间和照片占位，不删除到访记录。
- 底图丢失：尝试显示点线层；若渲染容器整体失败，显示静态图。
- 未安装适配器：保留普通代码块，使用标准 Markdown 图片和链接降级。

## 本轮不展示的交互

- 地图内直接编辑点位、拖动点位或修改照片引用。
- GPX 导入、停留点自动识别与照片时间匹配。
- 跨日、旅程或年度足迹汇总。
- 分享、公开发布、协作编辑与社交功能。
